.actions
  = link_to 'New gram', new_admin_gram_path, :class => "new"
%h2 Grams

#grams
  - @grams.each do |gram|
    .gram{ :id => "gram-#{gram.id}" }
      = link_to image_tag(gram.image.url(:thumb)), [:admin, gram]

.clearfix
